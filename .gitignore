# data/ 由运行时生成，勿手工编辑、勿纳入版本控制：
#   visits.count   <- nginx /api/visits Lua 块（每次访问自增）
#   status.json    <- /opt/homepage-status/status_check.sh（cron 每 5 分钟）
#   stats.json     <- /opt/homepage-status/stats_check.sh（cron 每 10 分钟）
data/
*.bak-*
